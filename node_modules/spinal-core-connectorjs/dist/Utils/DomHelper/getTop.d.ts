/**
 * real position of an object
 * @export
 * @param {HTMLElement} l
 * @return {*}  {number}
 */
export declare function getTop(l: HTMLElement): number;
declare global {
    function get_top(l: HTMLElement): number;
    function getTop(l: HTMLElement): number;
}
