/**
 * real position of an object
 * @export
 * @param {HTMLElement} l
 * @return {*}  {number}
 */
export declare function getLeft(l: HTMLElement): number;
declare global {
    function getLeft(l: HTMLElement): number;
    function get_left(l: HTMLElement): number;
}
